@model mostaan2.ViewModel.viewFileVM


@if (Model.type == 1)
{
    <img src="~/files/@Model.filename"  style="max-width:100%; max-height:500px; object-fit:scale-down" />
}
else
{
    <embed src="~/files/@Model.filename"  style="width:800px; height:500px; "  />
}

